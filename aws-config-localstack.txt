# Configuración para AWS CLI (~/.aws/config)
[profile localstack]
region = us-east-1
output = json
endpoint_url = http://localhost:4566

# Credenciales para AWS CLI (~/.aws/credentials)
[localstack]
aws_access_key_id = test
aws_secret_access_key = test